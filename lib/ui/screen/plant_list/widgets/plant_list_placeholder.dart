import 'package:flutter/material.dart';

class PlantListPlaceholder extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Center(
        child: Text("Plants not found"),
      );
}
